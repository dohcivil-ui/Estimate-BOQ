# 🧪 สคริปต์ทดสอบ Workflow จบรอบ — อาคารฟอกไตเทียม

> ใช้ทดสอบว่าระบบ cost-estimator-v2.html + Custom GPT ทำงานร่วมกันได้จริง
> แบบทดสอบ: อาคารฟอกไตเทียม โรงพยาบาลสุมาลย์ อ.กุสุมาลย์ จ.สกลนคร (28 แผ่น)

---

## ขั้นตอนที่ 1 — เปิดแบบ + ตั้งสเกล

### ทำในโปรแกรม cost-estimator-v2.html

1. เปิดเบราว์เซอร์ → เปิดไฟล์ `cost-estimator-v2.html`
2. กด **📂 เปิดไฟล์** → เลือก `hemodyalysis_Architec.pdf`
3. PDF จะโหลด → ใช้ปุ่ม ◀▶ หรือ dropdown เปลี่ยนหน้า
4. ไปหน้า **A-06** (แปลนพื้น สเกล 1:125)
5. กดเครื่องมือ **📐 ตั้งสเกล**
6. คลิกจุดแรก-จุดที่สองตามเส้นกริดที่รู้ระยะ (เช่น กริด①→② = **5.00 ม.**)
7. พิมพ์ `5` ในช่อง → สเกลถูกตั้ง
8. ตรวจสอบ: วัดระยะ①→⑥ ควรได้ **~26.00 ม.** (±2%)

**ผ่าน/ไม่ผ่าน:** ☐ สเกลตั้งได้ถูกต้อง วัดเส้นกริดได้ใกล้เคียง

---

## ขั้นตอนที่ 2 — วัดปริมาณจากแบบ

### 2.1 วัดพื้นที่อาคาร

1. เลือกเครื่องมือ **⬡ พื้นที่**
2. คลิกมุมอาคาร 4 จุด (ตาม Gridline A①, A⑥, C⑥, C①)
3. กด **Enter** จบ
4. กด **✎** ตั้งชื่อ: `พื้นที่อาคาร`
5. **ค่าที่ต้องได้:** ~364 ตร.ม. (26.00 × 14.00)

**ผ่าน/ไม่ผ่าน:** ☐ พื้นที่วัดได้ใกล้เคียง ±5%

### 2.2 วัดเส้นรอบรูปผนังภายนอก

1. เลือก **📏 ความยาว**
2. คลิกตามแนวผนังรอบอาคาร (ไล่จากมุมหนึ่งวนกลับมา)
3. กด Enter จบ → ตั้งชื่อ: `ผนังภายนอก`
4. **ค่าที่ต้องได้:** ~80 ม. → 2(26+14)

**ผ่าน/ไม่ผ่าน:** ☐ ความยาวใกล้เคียง ±5%

### 2.3 วัดผนังภายใน (ตัวอย่าง 2-3 เส้น)

1. วัดแนวผนังจาก A→C ตามกริด② → ตั้งชื่อ: `ผนังใน-แนวกริด2`
2. วัดแนวผนังจาก ①→② ตามกริด B → ตั้งชื่อ: `ผนังใน-แนวกริดB`

**ผ่าน/ไม่ผ่าน:** ☐ ความยาวสมเหตุสมผล

### 2.4 นับจำนวนเสา (จากแบบโครงสร้าง)

1. เปิดไฟล์ `hemodyalysis_Structure.pdf` → ไปหน้า **S-03** (แปลนฐาน)
2. ตั้งสเกลใหม่ (สเกลเดียวกัน 1:125 แต่อาจต้องตั้งใหม่เพราะ PDF คนละไฟล์)
3. เลือกเครื่องมือ **🔢 นับจำนวน**
4. คลิกตำแหน่ง F1,C1 ทุกจุด → Enter จบ → ตั้งชื่อ: `เสา C1`
5. **จำนวนที่ต้องนับได้:** ~35 จุด (±3)

**ผ่าน/ไม่ผ่าน:** ☐ นับเสาได้ครบ snap ช่วยจับจุดได้

### 2.5 วัดพื้นที่หลังคา (จากแบบสถาปัตย์ A-07)

1. เปิดกลับไป `hemodyalysis_Architec.pdf` หน้า A-07
2. วัดพื้นที่เส้นขอบหลังคา → ตั้งชื่อ: `หลังคา`
3. **ค่าที่ต้องได้:** ~507 ตร.ม. (ขอบหลังคา 17.30×29.30)

**ผ่าน/ไม่ผ่าน:** ☐ พื้นที่หลังคาใกล้เคียง

---

## ขั้นตอนที่ 3 — ส่งออกค่าวัดไป Custom GPT

1. ในแท็บ **วาด/วัด** → ดูที่ **📊 สรุปปริมาณรวม (ตามชื่อ)** — ตรวจค่าว่าถูก
2. กดปุ่ม **📋 ส่งออกค่าวัดไป GPT**
3. Modal เด้งขึ้น → กด **📋 คัดลอก**
4. **ตรวจ:** ข้อความที่ได้ต้องมีรายการครบ ค่าที่วัดถูก เช่น:

```
📋 ข้อมูลที่วัดจากแบบ (ส่งจากโปรแกรมประมาณราคา)
โครงการ: อาคารฟอกไตเทียม
สเกล: 1 ม. = 158.4 px

รายการที่วัดได้:
1. "พื้นที่อาคาร" — พื้นที่ = 364.00 ตร.ม. (1 shape)
2. "ผนังภายนอก" — ความยาว = 80.00 ม. (1 shape)
3. "เสา C1" — จำนวน = 35 จุด (1 shape)
4. "หลังคา" — พื้นที่ = 507.00 ตร.ม. (1 shape)
...
```

**ผ่าน/ไม่ผ่าน:** ☐ ข้อความครบถ้วน คัดลอกได้

---

## ขั้นตอนที่ 4 — ส่งเข้า Custom GPT แล้วคำนวณ

### ทำใน ChatGPT (Custom GPT: ผู้ช่วยถอดปริมาณงานก่อสร้าง)

1. เปิด Custom GPT
2. อัปโหลดรูปแบบโครงสร้าง (หน้า S-09 รายละเอียดฐาน/เสา/คาน/พื้น)
3. วางข้อความที่คัดลอกจากขั้นตอน 3
4. พิมพ์เพิ่มเติม:

```
ข้อมูลจากแบบโครงสร้าง:
- ฐาน F1: 1.50×1.50 ม. เหล็ก 10+10-DB16 ทั้ง 2 ทาง
- เสา C1: 0.25×0.25 ม. สูง 4.00 ม. เหล็ก 4-DB20 ปลอก 2ปRB9@0.20
- คาน B1: 0.25×0.60 ม. เหล็ก 5-DB20 บน+ล่าง, 3-DB16 ข้าง, ปลอก 1ปRB9@0.10
- พื้น PS: หนา 0.14 ม. พื้นสำเร็จรูป+ทับหน้า DB12@0.20 ทั้ง 2 ทาง

ช่วยคำนวณปริมาณเหล็กเสริม คอนกรีต ไม้แบบ ทุกหมวด
```

5. **GPT ต้องทำ:**
   - ถามยืนยันค่าที่อ่าน ✓
   - คำนวณทีละขั้น แสดงวิธีคิด ✓
   - สรุปเป็นตาราง ✓

6. เมื่อ GPT คำนวณครบ พิมพ์: **"ส่งออก JSON"**

7. **GPT ต้องตอบด้วย JSON ในรูปแบบ:**

```json
{
  "project": "อาคารฟอกไตเทียม โรงพยาบาลสุมาลย์",
  "factorF": 1.2768,
  "boq": [
    {"name":"ค่าแรง คอนกรีตฐานราก F1 ×35","unit":"ลบ.ม.","rate":421,"qty":23.63,"isMat":false,"thick":null,"waste":3},
    ...
  ]
}
```

**ผ่าน/ไม่ผ่าน:**
- ☐ GPT ถามยืนยันก่อนคำนวณ
- ☐ GPT แสดงวิธีคิดทีละขั้น
- ☐ GPT สรุปตารางครบ
- ☐ JSON ส่งออกได้ ไม่มี error
- ☐ JSON ตรงตามรูปแบบที่กำหนด

---

## ขั้นตอนที่ 5 — นำ JSON กลับเข้าโปรแกรม BOQ

### ทำในโปรแกรม cost-estimator-v2.html

1. คัดลอก JSON ที่ GPT ส่งออก (ทั้ง code block)
2. กลับไปโปรแกรม → แท็บ **BOQ**
3. กดปุ่ม **🤖 นำเข้าจาก AI**
4. วาง JSON ใน textarea → กด **✅ นำเข้า**
5. **ต้องเห็น:** ข้อความสีเขียว "✅ นำเข้า XX รายการสำเร็จ · Factor F = 1.2768 · โครงการ: ..."
6. ปิด modal → รายการ BOQ ปรากฏครบ

**ตรวจสอบ:**
- ☐ รายการ BOQ ครบตามที่ GPT ส่งมา
- ☐ ค่าแรง/ค่าวัสดุ แยกถูกต้อง (badge 👷/🧱)
- ☐ ปริมาณ (qty) ถูกต้อง
- ☐ Factor F ถูกตั้งตามค่าใน JSON
- ☐ ยอดรวมราคากลางแสดงถูก

---

## ขั้นตอนที่ 6 — Export + ตรวจสอบ

1. กรอก **ชื่อโครงการ**: อาคารฟอกไตเทียม โรงพยาบาลสุมาลย์
2. กรอก **หน่วยงาน**: โรงพยาบาลสุมาลย์
3. กรอก **สถานที่**: อ.กุสุมาลย์ จ.สกลนคร
4. กดปุ่ม **📊 Excel** → เปิดไฟล์ .xlsx ตรวจ:
   - ☐ มีชีต "BOQ" ครบทุกรายการ
   - ☐ มีชีต "รายการที่วัด" (shapes)
   - ☐ ยอดรวมตรงกัน
5. กดปุ่ม **🖨️ PDF** → Print Preview:
   - ☐ หัวกระดาษมีชื่อโครงการ
   - ☐ ตารางอ่านง่าย ภาษาไทยไม่แตก
6. กดปุ่ม **💾 บันทึกงาน** → ได้ไฟล์ .json
7. กด **📂 เปิดงาน** → โหลดไฟล์กลับมา:
   - ☐ รูปแบบ+shapes+BOQ ครบ

---

## ขั้นตอนที่ 7 — บันทึก Feedback

### สิ่งที่ต้องจดขณะทดสอบ:

| หัวข้อ | คำถาม | บันทึก |
|--------|-------|--------|
| สเกล | ตั้งง่ายไหม? Snap ช่วยไหม? | |
| วัดพื้นที่ | คลิกจุดแม่นไหม? ค่าถูกไหม? | |
| นับจำนวน | หาจุดเสาง่ายไหม? | |
| PDF หลายหน้า | สลับหน้าสะดวกไหม? | |
| 📋 ส่งออก GPT | ข้อความครบไหม? ก๊อปได้ไหม? | |
| Custom GPT | อ่านแบบได้ไหม? คำนวณถูกไหม? | |
| JSON จาก GPT | รูปแบบถูกไหม? มี error ไหม? | |
| 🤖 นำเข้า AI | นำเข้าได้ไหม? รายการครบไหม? | |
| Export Excel/PDF | ข้อมูลครบ? ภาษาไทยถูก? | |
| บันทึก/เปิดงาน | shapes+BOQ กลับมาครบไหม? | |

### Bug ที่พบ:
1. ...
2. ...
3. ...

### ข้อเสนอปรับปรุง:
1. ...
2. ...

---

## สรุป Checklist ผ่าน/ไม่ผ่าน

| # | รายการ | ผ่าน |
|---|--------|------|
| 1 | เปิด PDF + ตั้งสเกลถูก | ☐ |
| 2 | วัดพื้นที่อาคาร ~364 ตร.ม. | ☐ |
| 3 | วัดเส้นรอบรูปผนังนอก ~80 ม. | ☐ |
| 4 | นับเสา C1 ~35 จุด | ☐ |
| 5 | 📋 ส่งออกค่าวัดไป GPT ครบ | ☐ |
| 6 | GPT คำนวณถูก ส่ง JSON ออกมา | ☐ |
| 7 | 🤖 นำเข้า JSON เข้า BOQ สำเร็จ | ☐ |
| 8 | ราคากลางแสดงถูก (Factor F ×) | ☐ |
| 9 | Export Excel/PDF ข้อมูลครบ | ☐ |
| 10 | บันทึก/เปิดงาน ข้อมูลครบ | ☐ |

**ผ่านครบ 10/10 = ระบบพร้อมใช้งานจริง**
