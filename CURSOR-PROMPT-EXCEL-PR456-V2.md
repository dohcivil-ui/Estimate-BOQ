# 🎯 Cursor Prompt — แก้ Excel Export ให้ตรงตัวอย่าง ปร.4(ก) ปร.5 ปร.6

> คัดลอกทั้งหมดนี้วางใน Cursor

---

## งานที่ต้องทำ

**เขียน `src/services/govExcelExport.ts` ใหม่ทั้งไฟล์** ให้สร้าง Excel ตรงตามตัวอย่างจริงที่แนบ ลบโค้ดเดิมทั้งหมดแล้วเขียนใหม่ ไฟล์ Excel มี **4 sheets** ตามลำดับ:

1. **Factor F** — ตารางคำนวณ Factor F
2. **ปร.4(ก)** — รายการปริมาณงานและราคา (BOQ ละเอียด)
3. **ปร.5** — สรุปราคาค่าก่อสร้าง (สรุปต้นทุน × Factor F)
4. **ปร.6** — สรุปค่าก่อสร้าง (ราคาสุดท้าย + ลายเซ็น)

---

## Sheet 1: "Factor F"

### Layout (Column A-E):
```
Row 1: "ตารางแสดงการคำนวณหาค่า FACTOR F งานอาคาร" (merge A1:E1, bold, size 16)
Row 2: [ชื่อโครงการ] (merge A2:E2)
Row 3: "สถานที่ก่อสร้าง [สถานที่]" (merge A3:E3)
Row 4: "หน่วยงาน [หน่วยงาน]" (merge A4:E4)
Row 5: (ว่าง)
Row 6: "เงื่อนไข" (bold)
Row 7: A="เงินล่วงหน้าจ่าย (ร้อยละ)" | D=0.0000  ← Input cell (สีฟ้าอ่อน)
Row 8: A="ค่าประกันผลงาน หัก (ร้อยละ)" | D=0.0000
Row 9: A="ดอกเบี้ยเงินกู้ (ร้อยละ)" | D=0.0600  ← Input
Row 10: A="ค่าภาษีมูลค่าเพิ่ม VAT (ร้อยละ)" | D=0.0700  ← Input
Row 11: (ว่าง)
Row 12: "ตารางค่า Factor F มาตรฐาน (สำนักงบประมาณ)" (bold)
Row 13: A="ค่างานต้นทุน (บาท)" | B="ถึง (บาท)" | C="Factor F"
```

### ตาราง Factor F มาตรฐาน (Row 14-37):
```
| ค่างานต้นทุน (บาท) | ถึง (บาท)     | Factor F |
|---------------------|---------------|----------|
| 0                   | 500,000       | 1.3074   |
| 500,000             | 1,000,000     | 1.3074   |
| 1,000,000           | 2,000,000     | 1.3050   |
| 2,000,000           | 5,000,000     | 1.3035   |
| 5,000,000           | 10,000,000    | 1.3003   |
| 10,000,000          | 15,000,000    | 1.2943   |
| 15,000,000          | 20,000,000    | 1.2594   |
| 20,000,000          | 25,000,000    | 1.2518   |
| 25,000,000          | 30,000,000    | 1.2248   |
| 30,000,000          | 40,000,000    | 1.2164   |
| 40,000,000          | 50,000,000    | 1.2161   |
| 50,000,000          | 60,000,000    | 1.2159   |
| 60,000,000          | 70,000,000    | 1.2061   |
| 70,000,000          | 80,000,000    | 1.2050   |
| 80,000,000          | 90,000,000    | 1.2050   |
| 90,000,000          | 100,000,000   | 1.2049   |
| 100,000,000         | 150,000,000   | 1.2049   |
| 150,000,000         | 200,000,000   | 1.2023   |
| 200,000,000         | 250,000,000   | 1.2023   |
| 250,000,000         | 300,000,000   | 1.2013   |
| 300,000,000         | 350,000,000   | 1.1951   |
| 350,000,000         | 400,000,000   | 1.1866   |
| 400,000,000         | 500,000,000   | 1.1858   |
| 500,000,000         | 1,000,000,000 | 1.1853   |
```

### ส่วนคำนวณ (Row 39-53):
```
Row 39: (ว่าง)
Row 40: "สูตรคำนวณหาค่า Factor F = D − [(D − E) × (A − B) ÷ (C − B)]"
Row 41: (ว่าง)
Row 42: A="A = ค่างานต้นทุน (pull from ปร.4(ก))" | D=[สูตร ='ปร.4(ก)'!grand_total_cell]
Row 43: A="B = ค่างานตัวต่ำกว่าต้นทุน (lower bracket)" | D=[VLOOKUP หา bracket ล่าง]
Row 44: A="C = ค่างานตัวสูงกว่าต้นทุน (upper bracket)" | D=[VLOOKUP หา bracket บน]
Row 45: A="D = Factor F ของช่วงต่ำ" | D=[INDEX/MATCH จากตาราง]
Row 46: A="E = Factor F ของช่วงสูง" | D=[INDEX/MATCH จากตาราง]
Row 47: (ว่าง)
Row 48: A="Factor F (มาตรฐาน สงป.)" | D=[สูตรคำนวณ Factor F]  ← bold, สีเขียว
Row 49-51: (ว่าง)
Row 52: A="🔧 Factor F ที่ปรับตามเงื่อนไขจริง (custom)" | D=[Input cell ให้ user แก้ได้]
Row 53: A="**Factor F ที่จะใช้จริง (เลือก)**" | D=[=D48 หรือ =D52 แล้วแต่ user]  ← bold, สีเหลือง
```

**หมายเหตุ**: ค่า A (ค่างานต้นทุน) ต้องเป็นสูตร Excel ที่อ้างจากยอดรวมใน ปร.4(ก) — ไม่ hardcode

---

## Sheet 2: "ปร.4(ก)"

### Layout (Column A-L, 12 columns):
```
Row 1: "แบบ ปร.4(ก)" (merge A1:L1, bold, size 16, center)
Row 2: "รายการปริมาณงานและราคา" (merge A2:L2, bold, center)
Row 3: [ชื่อโครงการ] (merge A3:L3)
Row 4: A="สถานที่ [สถานที่]" | G="หน่วยงาน [หน่วยงาน]" (merge ตามเหมาะสม)
Row 5: A="ประมาณราคาโดย [ชื่อผู้ประมาณราคา] ([ตำแหน่ง])" | G="ประมาณราคาเมื่อวันที่ [วันที่]"
Row 6: (ว่าง)
```

### Table Header (Row 7-8, 2-level merge):
```
Row 7: A="ลำดับ\nที่" | B="รายการ"(merge B7:D7) | E="จำนวน" | F="หน่วย" | G="ค่าวัสดุ"(merge G7:H7) | I="ค่าแรงงาน"(merge I7:J7) | K="รวมค่าวัสดุ\nและค่าแรงงาน" | L="หมายเหตุ"
Row 8: G="ราคา/หน่วย" | H="จำนวนเงิน" | I="ราคา/หน่วย" | J="จำนวนเงิน"
```

### Column widths:
A=8, B=50(merge B:D), E=10, F=8, G=14, H=14, I=14, J=14, K=18, L=16

### Data rows — จัดกลุ่มตาม BOQ sections:

สำหรับแต่ละ section ใน BOQ:
```
Section header row: B="A. [ชื่อหมวด]" (bold, พื้นหลังสีเหลืองอ่อน #FFF2CC, merge B:D)
Item rows:
  A=ลำดับ (A1, A2, A3a, A3b... ตาม section letter + running number)
  B=ชื่อรายการ (merge B:D)
  E=จำนวน (number)
  F=หน่วย (text: m, m², ชุด, lot, กก., ฐาน, จุด, etc.)
  G=ค่าวัสดุ ราคา/หน่วย (number)
  H=ค่าวัสดุ จำนวนเงิน (formula: =E*G)
  I=ค่าแรง ราคา/หน่วย (number)
  J=ค่าแรง จำนวนเงิน (formula: =E*I)
  K=รวม (formula: =H+J)
  L=หมายเหตุ (text)
```

### Grand Total row (last row):
```
A="รวมค่าวัสดุและค่าแรงงานทั้งหมด" (merge A:F, bold)
H=SUM ของค่าวัสดุทั้งหมด (formula)
J=SUM ของค่าแรงทั้งหมด (formula)
K=SUM ของรวมทั้งหมด (formula, หรือ =H+J)
```
- Grand total: bold, เส้นคู่ด้านบน, พื้นหลังสีเขียวอ่อน #E2EFDA

### Section letters:
- ใช้ตัวอักษร A, B, C, D, E... ตามลำดับ section ที่มีใน BOQ
- ถ้า BOQ มีหมวด "งานพื้น" → จัดเป็น section A (หรือตามลำดับ)
- ถ้า BOQ มีหมวด "งานหลังคา" → จัดเป็น section B
- ลำดับย่อย: A1, A2, A3... B1, B2...
- ถ้ารายการย่อยซ้อน ใช้ suffix a, b, c (เช่น A3a, A3b)

### Formatting:
- Font: TH SarabunPSK size 14 (ถ้าไม่ได้ใช้ Sarabun หรือ Arial)
- Header size 16
- ทุกเซลล์มี border thin
- Section header: bold, สีเหลืองอ่อน
- Number format: #,##0 (ไม่มีทศนิยมสำหรับจำนวนเงิน ตามตัวอย่าง)
- Print: A4 landscape, repeat rows 7-8

---

## Sheet 3: "ปร.5"

### Layout (Column A-L):
```
Row 1: "แบบ ปร.5(ก)" (merge A1:L1, bold, size 16, center)
Row 2: "สรุปราคาค่าก่อสร้าง" (merge, bold, center)
Row 3: [ชื่อโครงการ] (merge)
Row 4: A="สถานที่ [สถานที่]" | G="หน่วยงาน [หน่วยงาน]"
Row 5: A="แบบ ปร.4(ก) ที่แนบ จำนวน 1 แผ่น" | G="ประมาณราคาเมื่อวันที่ [วันที่]"
Row 6: (ว่าง)
```

### Table Header (Row 7):
```
A="ลำดับที่" | B="รายการ"(merge B:G) | I="ค่างานต้นทุน\n(บาท)" | J="Factor F" | K="ค่าก่อสร้าง\n(บาท)" | L="หมายเหตุ"
```

### Data rows (Row 8+):
```
Row 8:  A="1" | B="ค่าก่อสร้าง[ชื่อโครงการ]" | I=[สูตรจาก ปร.4(ก) grand total] | J=[สูตรจาก Factor F sheet] | K=[=I8*J8]
Row 9:  (ว่าง)
Row 10: "เงื่อนไข" (bold)
Row 11: B="เงินล่วงหน้าจ่าย" | K=[สูตรจาก Factor F sheet D7]
Row 12: B="เงินประกันผลงานหัก" | K=[สูตรจาก Factor F sheet D8]
Row 13: B="ดอกเบี้ยเงินกู้" | K=[สูตรจาก Factor F sheet D9]
Row 14: B="ค่าภาษีมูลค่าเพิ่ม (VAT)" | K=[สูตรจาก Factor F sheet D10]
Row 15: (ว่าง)
Row 16: B="รวมค่าก่อสร้าง" (bold) | K=[=K8] (bold, เส้นคู่)
Row 17: B="ยอดสุทธิ (ราคาเสนอ)" | K=[=K16]
Row 18: (ว่าง)
```

### ส่วนเปรียบเทียบงบ (optional ถ้ามีข้อมูล):
```
Row 19: B="งบประมาณเป้า (net construction budget)" | K=[ถ้ามี]
Row 20: B="ค่างานต้นทุน vs งบ (net comparison)" | K=[=I8-K19]
Row 21: B="ผลลัพธ์ (net)" | K=[IF formula: ✓ ต่ำกว่างบ / ✗ สูงกว่างบ]
Row 22: B="ราคายื่น (Sale = net × Factor F)" | K=[=K16]
Row 23: B="Factor F ที่ใช้" | K=[=J8]
```

### ลายเซ็น (ท้ายสุด):
```
Row 25: A="ผู้ประมาณราคา" | C="…………………………………………" | G="ตำแหน่ง [ตำแหน่ง]"
Row 26: C="([ชื่อผู้ประมาณราคา])"
Row 27: (ว่าง)
Row 28: A="รับรองความถูกต้อง" | C="…………………………………………" | G="[ตำแหน่งผู้รับรอง]"
Row 29: C="(…………………………………………)"
```

---

## Sheet 4: "ปร.6"

### Layout (Column A-J):
```
Row 1: "แบบ ปร.6" (merge, bold, size 16, center)
Row 2: "สรุปราคาค่าก่อสร้าง" (merge, bold, center)
Row 3: [ชื่อโครงการ] (merge)
Row 4: A="สถานที่ [สถานที่]" | F="หน่วยงาน [หน่วยงาน]"
Row 5: A="แบบ ปร.4(ก) ปร.5(ก) ปร.6 และ Factor F ที่แนบ จำนวน 4 แผ่น" | F="ประมาณราคาเมื่อวันที่ [วันที่]"
Row 6: (ว่าง)
```

### Table Header (Row 7):
```
A="ลำดับที่" | B="รายการ"(merge B:F) | H="ค่าก่อสร้าง (บาท)"(merge H:I) | J="หมายเหตุ"
Row 8: B="สรุป" (bold)
```

### Data (Row 9+):
```
Row 9:  A="1" | B="ค่าก่อสร้าง[ชื่อโครงการ]" | H=[สูตรจาก ปร.5 K16]
Row 10: (ว่าง)
Row 11: "สรุป" | B="รวมค่าก่อสร้างเป็นเงินทั้งสิ้น" (bold, เส้นคู่) | H=[=H9] | J="**"
Row 12: A="([จำนวนเงินตัวอักษรไทย])" (merge A12:J12, italic)
```

**จำนวนเงินตัวอักษรไทย**: เขียนฟังก์ชัน `bahtText(amount)` ที่แปลงตัวเลขเป็นภาษาไทย เช่น:
- 499,516 → "สี่แสนเก้าหมื่นเก้าพันห้าร้อยสิบหกบาทถ้วน"
- 382,068 → "สามแสนแปดหมื่นสองพันหกสิบแปดบาทถ้วน"
- ใส่เป็น text (ไม่ใช่ formula)

### ลายเซ็น (Row 14+):
```
Row 14: A="ผู้ประมาณราคา" | C="…………………………………………" | G="ตำแหน่ง [ตำแหน่ง]"
Row 15: C="([ชื่อผู้ประมาณราคา])"
Row 16: (ว่าง)
Row 17: A="รับรองความถูกต้อง" | C="…………………………………………" | G="[ตำแหน่งผู้รับรอง]"
Row 18: C="(…………………………………………)"
Row 19: (ว่าง)
Row 20: A="ตรวจสอบความถูกต้อง" | C="…………………………………………" | G="[ตำแหน่งผู้ตรวจสอบ]"
Row 21: C="(…………………………………………)"
```

### หมายเหตุท้าย (Row 24+):
```
Row 24: "หมายเหตุ: หากเปลี่ยน Factor F → แก้ตัวอักษรจำนวนเงินใน A12 ด้วย"
Row 25: "ราคายื่น [จำนวนเงิน] บ. (ต้นทุน [ต้นทุน] × Factor F [factorF])"
```

---

## 5. ฟังก์ชัน bahtText(amount)

สร้างฟังก์ชันแปลงตัวเลขเป็นตัวอักษรไทย:

```typescript
function bahtText(amount: number): string {
  // ปัดเป็นจำนวนเต็ม
  const num = Math.round(amount);
  if (num === 0) return 'ศูนย์บาทถ้วน';
  
  const digits = ['', 'หนึ่ง', 'สอง', 'สาม', 'สี่', 'ห้า', 'หก', 'เจ็ด', 'แปด', 'เก้า'];
  const positions = ['', 'สิบ', 'ร้อย', 'พัน', 'หมื่น', 'แสน', 'ล้าน'];
  
  // แปลงกลุ่มละ 6 หลัก (ล้าน)
  // ใช้ logic มาตรฐาน: เอ็ด (ท้าย), ยี่สิบ, สิบ (ไม่มี หนึ่งสิบ)
  // คืน "[จำนวน]บาทถ้วน" หรือ "[จำนวน]บาท[สตางค์]สตางค์"
  
  // implement ให้ถูกต้องตามหลักภาษาไทย
  // เช่น 21 = ยี่สิบเอ็ด, 11 = สิบเอ็ด, 10 = สิบ, 1 = หนึ่ง
}
```

---

## 6. ข้อมูล project metadata ที่ต้องดึง

ดึงจาก project state/store ที่มีอยู่:

```typescript
interface ProjectMeta {
  projectName: string;      // ชื่อโครงการ
  location: string;         // สถานที่ก่อสร้าง
  agency: string;           // หน่วยงาน
  estimatorName: string;    // ชื่อผู้ประมาณราคา
  estimatorTitle: string;   // ตำแหน่ง
  date: string;             // วันที่ประมาณราคา
  budget?: number;          // งบประมาณ (ถ้ามี)
  factorF: number;          // Factor F ที่ใช้
  interestRate: number;     // ดอกเบี้ย (default 0.06)
  vatRate: number;          // VAT (default 0.07)
  advancePayment: number;   // เงินล่วงหน้า (default 0)
  retention: number;        // ประกันผลงาน (default 0)
}
```

ถ้า field ไหนไม่มีข้อมูล ให้ใส่ placeholder: "[ระบุ]" หรือ "…………………"

---

## 7. Cross-sheet formulas (สำคัญมาก!)

ทุกค่าตัวเลขที่ซ้ำกันข้าม sheet **ต้องเป็นสูตร Excel** อ้างข้ามไม่ hardcode:

```
'Factor F' D42 = 'ปร.4(ก)'!K[grand_total_row]     ← ค่างานต้นทุน
'ปร.5'    I8  = 'ปร.4(ก)'!K[grand_total_row]      ← ค่างานต้นทุน
'ปร.5'    J8  = 'Factor F'!D53                     ← Factor F ที่ใช้จริง
'ปร.5'    K8  = I8 * J8                            ← ค่าก่อสร้าง
'ปร.6'    H9  = 'ปร.5'!K16                         ← รวมค่าก่อสร้าง
```

---

## 8. BOQ items grouping

จัดกลุ่ม BOQ items เป็น sections ตามฟิลด์ `category` หรือ `group` ที่มีใน state:

- ถ้า category = "งานพื้น" → section A
- ถ้า category = "งานหลังคา" → section B
- ถ้า category = "งานผนัง" → section C
- ฯลฯ ตามลำดับที่มีใน BOQ

ถ้าไม่มี category → จัดเป็น section เดียว "A. รายการงาน"

Section letter ใช้ A, B, C, D, E... ตามจำนวน category ที่มี
Item number: A1, A2, A3... B1, B2...

---

## 9. UI — Dropdown ปุ่ม Export

ปุ่ม Export ใน BOQPanel ให้มี dropdown 3 ตัวเลือก:
- 📋 **ปร.4 + ปร.5 + ปร.6 + Factor F (ครบชุด)** ← default, highlight
- 📑 ปร.4(ก) อย่างเดียว
- 🗒️ BOQ แบบเดิม

ชื่อไฟล์: `ปร456_[ชื่อโปรเจกต์]_[YYYY-MM-DD].xlsx`

---

## 10. ข้อกำหนด

- **ลบ govExcelExport.ts เดิมทั้งไฟล์** แล้วเขียนใหม่ตาม spec นี้
- ทุกค่าเงินข้าม sheet ใช้ **สูตร Excel** ไม่ hardcode
- Number format: #,##0 (ไม่มีทศนิยม ตามตัวอย่าง)
- Font: TH SarabunPSK 14pt (body), 16pt (title)
- Border thin ทุกเซลล์ในตาราง
- Section header: bold, พื้นหลัง #FFF2CC
- Grand total: bold, พื้นหลัง #E2EFDA, เส้นคู่ด้านบน
- Input cells (Factor F sheet): พื้นหลังสีฟ้าอ่อน #DBEEF4
- Column B ใน ปร.4(ก) merge กับ C, D เสมอ (รายการยาว)
- ฟังก์ชัน bahtText ต้องถูกต้อง 100% ตามหลักภาษาไทย
- Print setup: A4, repeat header rows
- typecheck + lint ต้องผ่าน
