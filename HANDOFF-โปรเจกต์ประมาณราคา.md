# 📋 HANDOFF — โปรเจกต์โปรแกรมประมาณราคาก่อสร้าง

> เอกสารส่งต่อสำหรับเริ่มแชทใหม่ — แนบไฟล์นี้ + ไฟล์โปรแกรมล่าสุด แล้วบอก Claude ว่า
> "อ่าน HANDOFF นี้แล้วทำงานต่อ" เพื่อทำงานต่อโดยไม่ต้องเล่าใหม่

---

## 1) ภาพรวมโปรเจกต์

ผู้ใช้เป็น **วิศวกรโยธา จ.หนองคาย** กำลังสร้างเครื่องมือ **ประมาณราคางานก่อสร้าง** จากแบบ (JPG/PNG/PDF)
อ้างอิงบัญชีค่าแรง **ว.809 (14 พ.ย. 2568) กรมบัญชีกลาง** สื่อสารเป็นภาษาไทย

**ปรัชญา:** เริ่มจากเครื่องมือ standalone ใช้ได้ทันที → ค่อยขยายเป็น Web App + AI

---

## 2) ไฟล์ทั้งหมดในโปรเจกต์ (ล่าสุด)

| ไฟล์ | หน้าที่ | สถานะ |
|------|---------|-------|
| **cost-estimator-v2.html** | โปรแกรมหลัก (single-file HTML เปิดในเบราว์เซอร์) | ใช้งานได้ Phase 1 ครบ |
| **คู่มือการใช้งาน.html** | คู่มือแจกทีม (พิมพ์เป็น PDF ได้) | ต้องอัปเดตทุกครั้งที่แก้โปรแกรม |
| **Custom-GPT-ตั้งค่า.md** | คู่มือ + Instructions สร้าง Custom GPT (QTO agent) | ใช้งานได้ |
| **ฐานความรู้-ถอดปริมาณครบทุกหมวด.md** | Knowledge อัปโหลดเข้า Custom GPT | ใช้งานได้ (ยังเพิ่มตัวอย่างได้) |
| **ราคาวัสดุ-ไกด์รายจังหวัด-template.csv** | ตารางราคาไกด์ แก้เอง | template |

> ไฟล์เก่าที่เลิกใช้: cost-estimator.html, cost-estimator.jsx, ฐานความรู้-คำนวณเหล็กเสริม.md (ถูกแทนด้วยไฟล์ครบทุกหมวด)

---

## 3) ความสามารถของโปรแกรม (Phase 1 — เสร็จ 100%)

**เปิดไฟล์:** JPG, PNG, PDF (เลือกหน้า + DPI). DWG/DWF ต้อง Save เป็น PDF ก่อน
**เครื่องมือวัด/วาด:** ⬡พื้นที่ · 📏ความยาว (โชว์ระยะเหนือเส้น) · ⭕วงกลม · 🔢นับจำนวน · 📐ตั้งสเกล · 🖱️เลือก
**Snap (F3):** ●ปลาย ▲กลาง ✕จุดตัด ⊾ตั้งฉาก ◇บนเส้น + **✛ Snap เส้นในแบบ** (ตรวจจับพิกเซลเส้นเข้มในรูป ปรับความไวได้)
**Ortho:** Shift ค้าง หรือ F8 (ล็อก x/y/45°)
**แก้ไข:** ลากย้าย vertex · Dbl-click บนเส้น = แทรกจุด · คลิกขวา = ลบจุด · Ctrl+D/⧉ = คัดลอก · ✎ ตั้งชื่อ
**Layer:** หลายชั้นงาน เปิด/ปิด ย้ายรูปข้ามชั้น
**หลายสเกลต่อหน้า:** แต่ละรูปจดจำสเกลของตัวเอง (s.sc)
**สรุปปริมาณรวมตามชื่อ:** กลุ่มชื่อเดียวกันรวมกัน
**BOQ:** เพิ่มจากค่าแรง(ว.809)/ค่าวัสดุ · ปริมาณ · ความหนา(→ปริมาตร ลบ.ม.) · เผื่อเสีย% · Factor F → ราคากลาง
**Export:** Excel(.xlsx), CSV, PDF(พิมพ์ผ่านเบราว์เซอร์ รองรับไทย) + หัวกระดาษ/ลายเซ็น
**บันทึก/เปิดงาน:** ไฟล์ .json พกพาข้ามเครื่อง (รวมรูป+สเกล+รูปวาด+BOQ+layer+Factor F)
**🤖 นำเข้าจาก AI:** วาง JSON จาก Custom GPT → สร้างรายการ BOQ อัตโนมัติ
**คีย์ลัด:** F3,F8,Shift,Enter(จบ),Esc,Ctrl+Z,Ctrl+D,Delete, ล้อ=ซูม, Alt+ลาก=pan

---

## 4) สถาปัตยกรรมเทคนิคของ cost-estimator-v2.html

- **โครงสร้าง:** single-file HTML, theme เข้ม, ฟอนต์ Sarabun
- **CDN:** Sarabun (Google Fonts), SheetJS xlsx 0.18.5, PDF.js 3.11.174 — ต้องต่อเน็ตครั้งแรก
- **State หลัก:** `tool, shapes[], boq[], sc(scale px/m), layers[], activeLayer, factorF, snap toggles (snapEnd/Mid/Int/Perp/Near/snapImg), imgData(พิกเซลรูป)`
- **shape มี:** type(polygon/line/circle/count/calibrate), points[], areaSqm/lengthM/radiusM/circM/count, name, label, layer, sc
- **ฟังก์ชันสำคัญ:** draw(), fSnap(pt,exclSi), imgSnap(pt), recalcShape(s), addShape(s), rDraw/rBoq/rRates/rMats, effQty/lineTotal/sumBOQ, expXL/expCSV/expPDF, saveProject/loadProject, importAI/doImportAI, showGuide
- **ตรวจ syntax:** ดึง <script> ก้อนใหญ่สุดมารันด้วย `new Function(main)` ใน node + เช็ค <div> เปิด/ปิดสมดุล

**รูปแบบ JSON สำหรับ importAI (สำคัญ — ใช้กับ Custom GPT):**
```json
{ "project":"ชื่อ", "factorF":1.3,
  "boq":[ {"name":"...","unit":"ตัน|ลบ.ม.|ตร.ม.|เมตร|ชุด","rate":3900,"qty":0.38,"isMat":false,"thick":null,"waste":7} ] }
```

---

## 5) ฝั่ง AI — Custom GPT (อยู่บน ChatGPT แยกจากโปรแกรม)

- เป็น **agent ถอดแบบ**: สำรวจแบบ → ถาม-ยืนยัน-คำนวณ ทีละหมวด → ออก BOQ → ส่งออก JSON
- Knowledge = ฐานความรู้-ถอดปริมาณครบทุกหมวด.md (เหล็ก/คอนกรีต/ไม้แบบ/ก่อ/ฉาบ/ทาสี/หลังคา/ระบบ + ว.809 + ราคาไกด์)
- ต้องเปิด Code Interpreter
- **ราคา = ไกด์ ไม่ตายตัว** (ตลาดผันผวน) แจ้งให้บวก VAT 7% + ขนส่ง, สืบราคาจริงรายจังหวัด
- เชื่อมกับโปรแกรม = ก๊อป JSON → ปุ่ม 🤖 นำเข้าจาก AI (ยังไม่ auto เพราะ GPT cloud แตะไฟล์ local ไม่ได้)

**แหล่งราคาทางการ:** สนค. กระทรวงพาณิชย์ — index.tpso.go.th/construction-material-prices (รายจังหวัด, ย้อนหลัง 1 เดือน, ไม่รวม VAT+ขนส่ง)

---

## 6) งานที่ค้าง (ทำต่อในแชทใหม่)

### งาน A — เพิ่มตัวอย่างถอดปริมาณในฐานความรู้ GPT
เพิ่มตัวอย่างคำนวณละเอียด (worked example) สำหรับ: **เสา C1, คาน B1, พื้น S1, บันได** ในไฟล์ ฐานความรู้-ถอดปริมาณครบทุกหมวด.md (ส่วนที่ 3 ตอนนี้มีแต่ฐานราก F1) — ให้ GPT คำนวณแม่นขึ้น
- เสา: คอนกรีต=b×h×H, เหล็กยืน=n×(H+ทาบ40d), ปลอก=⌊H/ระยะ⌋+1 เส้น
- คาน: คอนกรีต=b×h×L, ไม้แบบ=(2h+b)×L, เหล็กหลัก+ปลอก
- พื้น: คอนกรีต=A×t, เหล็กตะแกรง 2 ทาง หรือ wire mesh
- บันได: ขั้น+พื้นเอียง — ให้ใส่สูตรปริมาตรคอนกรีตและเหล็ก

### งาน B — Prompt ทดสอบจำลองถอดแบบ 1 อาคาร
เขียนสคริปต์บทสนทนาตัวอย่าง (อาคาร คสล. ชั้นเดียวเล็กๆ) แสดง GPT: สำรวจแบบ→ถามยืนยันแต่ละหมวด→คำนวณ→ออก JSON ครบ เพื่อใช้สาธิต/เทรนทีม

### งาน C — Phase 2 Web App (งานใหญ่ ทำใน Claude Code)
- **Frontend:** React + Vite (ย้าย logic จาก HTML เดิม)
- **Backend:** Supabase (เร็วสุด) หรือ Node/Express
- **DB:** PostgreSQL — ตาราง: users, projects, boq_items, shapes, material_prices, delete_requests
- **Auth:** Google/Facebook OAuth (Supabase Auth)
- **RBAC:** Admin (เพิ่ม/แก้/ลบ/อนุมัติ/จัดการ user ทุกอย่าง) vs User (สร้าง/แก้/ใช้ tools ได้ แต่ **ลบไม่ได้ → ส่งคำขอลบให้ admin อนุมัติ**)
- **GPT Actions:** ให้ Custom GPT เรียก API backend ส่ง BOQ เข้าระบบกลางได้
- **ดึงราคา สนค. อัตโนมัติ:** scheduled job ดึง/อัปเดต material_prices รายจังหวัดรายเดือน (สนค.ไม่มี API → อาจต้อง scrape)
- **Hosting:** Vercel (frontend) + Supabase (backend) — มี free tier
- เก็บไฟล์แบบ/โปรเจกต์บน cloud storage

---

## 7) ข้อตกลง/หลักการที่ต้องจำ

- ทุกครั้งที่แก้โปรแกรม → **อัปเดต คู่มือการใช้งาน.html ให้ตรงกันเสมอ**
- ตรวจ syntax ทุกครั้งหลังแก้โค้ด (node new Function + div balance)
- ราคาวัสดุ/ค่าแรงไม่ทำ auto ตายตัว — เป็นไกด์ที่แก้ได้
- AI ใช้ช่วยร่าง ต้องมีคนตรวจ + ระบบถามยืนยันเสมอ
- ภาษาไทยทั้งหมด, UI theme เข้ม, ฟอนต์ Sarabun

---

## 8) วิธีเริ่มแชทใหม่

1. เปิดแชทใหม่ในโปรเจกต์เดียวกัน (Claude จะจำ memory หลักได้)
2. แนบไฟล์นี้ + cost-estimator-v2.html (และไฟล์อื่นที่จะแก้)
3. บอก: "อ่าน HANDOFF แล้วทำงานต่อ งาน A/B/C ..."
4. งาน C (Phase 2) แนะนำทำใน **Claude Code** เพราะเป็นโปรเจกต์หลายไฟล์
